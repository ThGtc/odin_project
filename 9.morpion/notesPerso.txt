
/*

1/ les mécanismes du jeu :

> le plateau de jeu (une grille 3x3) sera stocké dans un Array, rangé dans un objet 'gameboard'
    array que l'on peut imaginer comme ça [O, O, ] pour une rangée... et ainsi de suite sur trois rangées
> les deux joueurs aussi seront rangés dans des objets distincts
> et il faudra sûrement un objet pour gérer le flux de jeu (comment la partie évolue ?)

/!\ objectif sous-jacent : keep it simple stupid ! le but du jeu est d'avoir le moins de code possible
    > concrètement, si tu as besoin d'UN quelque chose (plateau, panneau d'affichage...) > module
    > et si tu as besoin de plusieurs quelque chose (joueurs ;) ) > factories

2/ l'affichage :

> mettre en place le html
> puis une fonction JS qui affichera le contenu du array sur la page
    (sûrement une position dans le array, une cellule ?)
> faire tirer à blanc pour tester

[x,x,x,x,x,x,x,x,x] > autant de choix dans l'array que de cases, puis sur le html une grille de 3x3 et chaque cellule = une place dans le array

3/ un tour de jeu :

> mettre en place les fonctions permettant aux joueur de placer ses pions sur la grille (via le DOM)
> pas oublier que si une cellule est prise = impossible :)

4/ la logique de fin de partie :

> permettre de vérifier soit un match nul (plus rien ne peut-être poser ou victorieux)
> soit un gagnant (avec son pion atitré, ses points, etc...)
> et un petit message pour le vainqueur :)

5/ le polish :

> un bouton pour recommencer, un bouton pour clear le leaderboard...
> la possibilité pour les joueurs de rentrer leurs pseudos...
> et un leaderboard :)

6/ l'IA ?

> plus tard, pas trop vite

*/
